const state = {
  web3: null,
  splitterInstance: null
}

const getters = {
}

const mutations = {
  setWeb3: (state, payload) => {
    state.web3 = payload
  }
}

const actions = {
}

export default {
  state,
  getters,
  mutations,
  actions
}
